[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "junit-time-diff"
dynamic = ["version"]
description = "Compare test execution times from JUnit XML reports."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    { name = "Mauricio Villegas", email = "mauricio@omnius.com" },
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
]

[project.scripts]
junit-time-diff = "junit_time_diff:main"

[project.optional-dependencies]
test = [
    "pytest>=8.3.5",
    "pytest-cov>=6.1.1",
]

[tool.setuptools]
py-modules = ["junit_time_diff"]

# Single source of truth for the version, so that bumpversion only needs to edit the module.
[tool.setuptools.dynamic]
version = { attr = "junit_time_diff.__version__" }

[tool.pytest.ini_options]
testpaths = ["tests"]


[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = [
    "E", "W",  # https://pypi.org/project/pycodestyle
    "F",  # https://pypi.org/project/pyflakes
    "I",  # https://pypi.org/project/isort
]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.black]
line-length = 120

[tool.typos.default.extend-identifiers]
Villegas = "Villegas"
